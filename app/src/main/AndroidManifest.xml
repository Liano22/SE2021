<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".signUp.SignUpView" />
        <activity android:name=".logIn.LogInView" />
        <activity android:name=".dogCreation.DogCreationView" />
        <activity android:name=".dashboard.DashboardPresenter" />
        <activity android:name=".matches.MatchesView" />
        <activity android:name=".likes.LikesView" />
        <activity android:name=".filter.FilterView" />
        <activity android:name=".dogSearch.DogSearchView"/>
        <activity android:name=".dashboard.DashboardView">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>